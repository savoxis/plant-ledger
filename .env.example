# Copy this to .env (which is gitignored) and set a real password.
# Any username works with HTTP Basic Auth -- only the password is checked.
# Leave unset / empty to run with no app-level lock (NPM Access List only).
AUTH_PASSWORD=
